DB 저장방식 2가지 : 1. 관계형DB(RelationDB)(엑셀처럼 형식틀에 저장)  2. NoSQL(Not only SQL메모장식으로 저장)
sql=dB
포엔드 테니스용어(포핸드, 백핸드)

프론트엔드 백엔드

1. extends(상속):부모에서 선언/정의를 모두 하며 자식은 메소드/변수를 그대로 사용할 수 있음
2. implement (interface 구현)(다중상속) : 부모 객체는 선언만 하며 정의(내용)은 자식에서 오버라이딩 해야 함.
3. abstract(추상적인 관념적인) : extends와 interface 혼합. extend하되 몇 개는 추상메서드로 구현되있음
     absolute :절대적

 [ C++, java, C# ] 상속, 다중상속 및 추상(abstract)클래스에 따른 고찰

  java, c#의 인터페이스 상속은 계약 및 분류의 의미강함  

immutable : 변하지 않는
1. (Const)const : 필드나 로컬 변수선언을 변경시 사용하고, 선언된 필드나 로컬변수는 변경된수 없음을 지정한다.
              -compile time의 constant 있고 
2. (Readionly)readonly : const와  역할은 같지만 초기화 방법 조금 차이가 있다. 선언과 동시에 초기화 할 수도 있고,
생성자 등에서 초기화 가능하다.  - runtime의 constant로 사용될 수 있다.
 오후 12:20 2023-08-30
3. literal(리터럴) : 소스코드이 고정된 값<=노골적인, 직관적인값>  하드코딩된 값] 을 대표하는 용어  

4. getmethod : 지정된 바인딩 제약 조건과 지정된 호출 규칙을 활용하여, 
지정된 제네틱 매개 변수의 수, 인수형식 및 
```	 한정자와 매개 변수가 일치하는 지정된  메서드를  검색한다.


오버라이딩: 재정의-그대로 가져다 (상속됨) 쓴다.
 - 부모클래스 가지고 있느 메소드의 틀(리턴타입, 메소드명, 매개변수)그대로 가지고 와서 {} 안쪽의 로직만 재정의하는 기법

오버로딩: 중복정의

캐스팅(Casting): 기존 data 타입을 다른 data 타입으로 변환하는것
/객체의 형변환은 상속이 전제되어 있다.
※primitive type: data 공간의 크기 :자동 형변환, 강제 형변환
※Reference type: 객체 내필드, 메소드의 접근 권한(업캐스팅, 다운캐스팅)


놀고싶다 놀고 싶다 격하게 놀고 싶다

영양제 : 수면 질 과 양이  떨어지고, 눈(시력) 피로감. 몸도 피로감 있고, 
 눈 영양제, 

호주, 윔블던, us오픈, (프랑스)롤랑가로스(4대 메이저대회)
호주, 윔블던, us오픈, 프랑스
라켓가방: 보온효과, 여름엔 습하고 라켓프레임 휘는것 방지 위해서 방습제 그대로 둔다